/**
 * VideoSrcInputDialogComponent.js
 * 
 * Dialog box to configure settings 
 */

// Node modules
import React from 'react';

// Locals


export default class VideoSrcInputDialogComponent extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return <div></div>
    }
}

VideoSrcInputDialogComponent.propTypes = {
    settings: PropTypes.object.isRequired //
}